- defaults:
    link:
      create: true
      relink: true

- clean: ['~']

- link:
    ~/.bash_profile:
    ~/.bashrc:
    ~/.gitconfig:
    ~/.gitignore_global:
    ~/.hyper.js:
    ~/.shell:
    ~/.vimrc:
    ~/.zshrc:
