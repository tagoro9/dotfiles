- defaults:
    link:
      create: true
      relink: true

- clean: ['~']

- link:
    ~/.bash_profile:
    ~/.bashrc:
    ~/.gitconfig:
    ~/.gitignore_global:
    ~/.hyper.js:
    ~/.p10k.zsh:
    ~/.shell:
    ~/.vimrc:
    ~/.zshrc:
    ~/bin/:
      glob: true
      path: bin/*
      relink: true
