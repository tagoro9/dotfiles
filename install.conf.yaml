- defaults:
    link:
      create: true
      relink: true

- clean: ['~']

- link:
    ~/.bash_profile:
    ~/.bashrc:
    ~/.gitconfig:
    ~/.gitignore_global:
    ~/.gnupg/gpg-agent.conf: gpg-agent.conf
    ~/.hyper.js:
    ~/.p10k.zsh:
    ~/.shell:
    ~/.tmux.conf:
    ~/.tmux.conf.local:
    ~/.vimrc:
    ~/.zshrc:
    ~/bin/:
      glob: true
      path: bin/*
      relink: true
